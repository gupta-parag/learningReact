import React from "react";
import ReactDOM from "react-dom/client";

const root = ReactDOM.createRoot(document.querySelector("#root"));
root.render(
  <React.StrictMode>
    <h1>Hello! this is my first time writting a React Application</h1>
  </React.StrictMode>
);
